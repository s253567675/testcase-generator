# ============================================
# 测试用例生成器 - 腾讯云部署环境变量配置
# ============================================
# 复制此文件为 .env 并填写实际值
# cp deploy/env.template .env

# ============================================
# 数据库配置（必填）
# ============================================
# MySQL 连接字符串格式：mysql://用户名:密码@主机地址:端口/数据库名
# 腾讯云 MySQL 示例：mysql://root:YourPassword123@cdb-xxxxxx.cd.tencentcdb.com:3306/testcase_db
DATABASE_URL=mysql://root:your_password@localhost:3306/testcase_generator

# ============================================
# JWT 安全配置（必填）
# ============================================
# 用于签名 JWT Token，请使用随机字符串（至少32位）
# 生成方法：node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your_jwt_secret_key_at_least_32_characters_long

# ============================================
# 服务器配置
# ============================================
# 服务器运行端口（默认3000）
PORT=3000

# 运行环境：development 或 production
NODE_ENV=production

# ============================================
# OAuth 认证配置（可选 - 腾讯云部署建议禁用）
# ============================================
# 设置为 true 启用 OAuth 登录，false 仅使用账号密码登录
ENABLE_OAUTH=false

# 以下配置仅在 ENABLE_OAUTH=true 时需要
# OAUTH_SERVER_URL=https://your-oauth-server.com
# VITE_OAUTH_PORTAL_URL=https://your-oauth-portal.com
# VITE_APP_ID=your_app_id

# ============================================
# AI 服务配置（可选）
# ============================================
# 内置 AI 服务（腾讯云部署时留空，使用自定义模型）
BUILT_IN_FORGE_API_URL=
BUILT_IN_FORGE_API_KEY=

# 前端 AI 服务（腾讯云部署时留空）
VITE_FRONTEND_FORGE_API_URL=
VITE_FRONTEND_FORGE_API_KEY=

# ============================================
# 文件存储配置
# ============================================
# 存储类型：local（本地存储）或 cos（腾讯云COS）
STORAGE_TYPE=local

# 本地存储路径（STORAGE_TYPE=local 时使用）
LOCAL_STORAGE_PATH=./uploads

# 腾讯云 COS 配置（STORAGE_TYPE=cos 时使用）
# COS_SECRET_ID=your_secret_id
# COS_SECRET_KEY=your_secret_key
# COS_BUCKET=your-bucket-1234567890
# COS_REGION=ap-guangzhou

# ============================================
# 应用信息配置
# ============================================
VITE_APP_TITLE=测试用例生成器
VITE_APP_LOGO=

# ============================================
# 管理员账号配置
# ============================================
# 首次启动时自动创建的管理员账号
ADMIN_USERNAME=admin
ADMIN_PASSWORD=admin123

# ============================================
# 其他配置
# ============================================
# 所有者信息（可选）
OWNER_NAME=管理员
OWNER_OPEN_ID=
